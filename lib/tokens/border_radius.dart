/*
This file contains all the border radius used in the app.
It imports the sizes.dart file to get the sizes.
*/

// Path: lib/tokens/border_radius.dart
import 'package:flutter/material.dart';
import 'sizes.dart';

class EmpyloBorderRadius {
  static const BorderRadius xs = BorderRadius.all(Radius.circular(Sizes.xs));
  static const BorderRadius s = BorderRadius.all(Radius.circular(Sizes.s));
  static const BorderRadius sm = BorderRadius.all(Radius.circular(Sizes.sm));
  static const BorderRadius m = BorderRadius.all(Radius.circular(Sizes.m));
  static const BorderRadius ml = BorderRadius.all(Radius.circular(Sizes.ml));
  static const BorderRadius l = BorderRadius.all(Radius.circular(Sizes.l));
  static const BorderRadius xl = BorderRadius.all(Radius.circular(Sizes.xl));
  static const BorderRadius xxl = BorderRadius.all(Radius.circular(Sizes.xxl));
  static const BorderRadius mega = BorderRadius.all(Radius.circular(Sizes.mega));
  static const BorderRadius giant = BorderRadius.all(Radius.circular(Sizes.giant));
  static const BorderRadius jumbo = BorderRadius.all(Radius.circular(Sizes.jumbo));
  static const BorderRadius huge = BorderRadius.all(Radius.circular(Sizes.huge));
  static const BorderRadius round = BorderRadius.all(Radius.circular(1000));
}